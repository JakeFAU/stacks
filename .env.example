# Generate independent local-only passwords, for example with:
# openssl rand -hex 24
STACKS_DB_ADMIN_PASSWORD=
STACKS_DB_APP_PASSWORD=

# Change this only if port 5432 is already occupied on the host.
STACKS_DB_PORT=5432

# AWS Bedrock Token
AWS_BEARER_TOKEN_BEDROCK=

